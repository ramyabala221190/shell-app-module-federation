{
    "${remoteAName}": "http://${hostName}:${ingressPort}/${remoteAName}/remote-${remoteAName}.js",
    "${remoteBName}":"http://${hostName}:${ingressPort}/${remoteBName}/remote-${remoteBName}.js"
}
  