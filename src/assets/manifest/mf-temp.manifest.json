{
    "${remoteAName}": "http://${hostName}-${env}:${ingressPort}/${remoteAName}/remote-${remoteAName}.js",
    "${remoteBName}":"http://${hostName}-${env}:${ingressPort}/${remoteBName}/remote-${remoteBName}.js"
}
  