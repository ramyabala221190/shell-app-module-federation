{
    "${remoteAName}": "http://${env}-${hostName}:${ingressPort}/${remoteAName}/remote-${remoteAName}.js",
    "${remoteBName}":"http://${env}-${hostName}:${ingressPort}/${remoteBName}/remote-${remoteBName}.js"
}
  