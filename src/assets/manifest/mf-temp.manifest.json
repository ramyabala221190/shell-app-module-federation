{
    "${remoteAName}": "${protocol}://${env}-${hostName}:${ingressPort}/${remoteAName}/remote-${remoteAName}.js",
    "${remoteBName}":"${protocol}://${env}-${hostName}:${ingressPort}/${remoteBName}/remote-${remoteBName}.js"
}
  